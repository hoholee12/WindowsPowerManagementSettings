apply this registry and you can find several new settings for the processor, in windows APM.

apply both min/max to 0(probably already default) core parking allowed settings to disable core parking

for other settings, refer to https://msdn.microsoft.com/en-us/library/windows/hardware/dn613985(v=vs.85).aspx